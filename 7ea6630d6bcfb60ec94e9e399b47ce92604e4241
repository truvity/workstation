CORRECTION (2026-09-21): this commit's message says "Replaces: v2.25.0". That is wrong — this repository was on v2.28.0 before the bump. The line came from a parallel sweep across several repositories that shared one scratch file for the commit message; the repository, the pin it moved to (v3.0.1) and everything else in the message are right, and the pull request body was correct.

The message is not rewritten because the commit has published releases descending from it, and rewriting it would leave those artifacts pointing at commits this repository no longer contains — a real loss of provenance to correct a wrong version number in prose.
